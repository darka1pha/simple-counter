import { NewComponentTypes } from "./NewComponent.types";

const INITIAL_STATE = {
  //  Your Initial State
};

const NewComponentReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default NewComponentReducer;
